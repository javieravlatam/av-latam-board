# Backups temporales de migración
*.bak_*

# macOS
.DS_Store

# Archivos temporales de editor
*.swp
*.tmp
*~

# Archivos de entrada (procesados localmente, no versionar)
inbox/

# Python cache
__pycache__/
*.pyc

# Versiones locales de datos (snapshots generados localmente)
versions/base/
versions/data/
versions/logs/

# Pipeline — artefactos derivados (regenerables desde inbox)
pipeline/raw_registry.json
pipeline/canonical/*.parquet
pipeline/canonical/*.csv
